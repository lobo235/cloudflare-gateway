# Cloudflare API token (scoped to DNS edit).
CF_API_TOKEN=your-cloudflare-api-token-here

# Default zone ID (optional — can be overridden per-request).
CF_ZONE_ID=

# Bearer token callers must present to use this API.
GATEWAY_API_KEY=your-gateway-api-key-here

# Port this gateway listens on. Default: 8080
PORT=8080

# Log level: debug, info, warn, error. Default: info
LOG_LEVEL=info
